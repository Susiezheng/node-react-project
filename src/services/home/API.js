'use strict'
const url = `/zsq`;
const API = {
    getSearch: `${url}/getSearch`,
    postLogin: `${url}/logins`,
    postSignUp: `${url}/signUps`,
    updatePwd: `${url}/updatePwd`,
    deletePwd: `${url}/deletePwd`
}

export default API;
